# Pinned for reproducibility — bump deliberately when a new feature is needed
# or a CVE forces it. Same discipline as the compose image tags in .env.example.
hcloud==2.2.0        # official Hetzner Cloud Python SDK
PyYAML==6.0.2        # read ondemand-pools.yaml
boto3==1.35.49       # AWS EC2 SDK — Graviton arm64 fallback workers (aws.enabled)
